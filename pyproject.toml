[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pcos"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
pcos = "pcos.cli:main"
